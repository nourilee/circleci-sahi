version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      
# Debug - Verify Ant version
      - run: ant -version
    
# Debug
      - run: pwd && ls -r
      - run: nc -vz 112.201.136.23 9999
         
# Run tests - Playback via ant
      - run: ant -f build.xml